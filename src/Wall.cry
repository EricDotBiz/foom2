class Wall{
    field x;
    field y;
    field width;
    field height;

    constructor(x, y, width=20, height=20){ 
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }
}